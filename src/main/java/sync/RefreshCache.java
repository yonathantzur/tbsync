package sync;

public class RefreshCache extends Sync {
    public RefreshCache() {
        super("Cache", "Refresh");
    }
    public RefreshCache(String message) {
        super("Cache", message);
    }
}
